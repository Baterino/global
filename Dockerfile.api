# API — build from monorepo root: docker build -f Dockerfile.api .
FROM node:22-bookworm-slim
WORKDIR /app

COPY apps/api/package.json ./package.json
RUN npm install

COPY apps/api/ ./

RUN npm run build \
  && npm prune --omit=dev

ENV NODE_ENV=production
EXPOSE 3001
CMD ["node", "dist/index.js"]
